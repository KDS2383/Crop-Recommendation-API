fastapi>=0.70
uvicorn[standard]>=0.15 # Use standard for WebSocket/other support if needed later
pydantic>=1.8
pandas==2.2.1
numpy==1.26.4
scikit-learn==1.4.2
xgboost==2.0.3
joblib>=1.0
requests>=2.25 # Keep if needed for other parts, though not for core rec here
pyarrow>=6.0
psutil>=5.8.0 
